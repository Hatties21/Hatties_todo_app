{
  "name": "todox",
  "private": true,
  "scripts": {
    "prebuild": "npm ci --prefix frontend --include=dev && npm ci --prefix backend --include=dev",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "start": "npm run start --prefix backend",
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "cross-env": "^7.0.3"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "jsonwebtoken": "^9.0.2",
    "react-router-dom": "^7.9.4"
  }
}
